Place your text files here. They will be copied into the docker
container, to the `text/input-files` directory. Your job will run in
parallel, if you provide many smaller files, instead of one big file.
